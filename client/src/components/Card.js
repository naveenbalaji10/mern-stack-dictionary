import React from 'react'

const EmptyCard = () => {
  return (
    <h3
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        color: '#444',
        height: '100vh',
      }}
    >
      Your Search List is empty !
    </h3>
  )
}

export default EmptyCard
